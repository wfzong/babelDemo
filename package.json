{
  "name": "babelpractice",
  "version": "1.0.0",
  "description": "babelpractice",
  "repository": {
    "type": "git",
    "url": "https://github.com/wfzong/babelDemo.git"
  },
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib -s",
    "build-watch": "babel src -d lib -s --watch",
    "build-single": "babel src --out-file lib-single/main.js -s"
  },
  "author": "wfz123@126.com",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-syntax-dynamic-import": "^6.18.0",
    "babel-preset-env": "^1.6.1",
    "cross-env": "^5.1.4"
  }
}
